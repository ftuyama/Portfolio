<h1>New project</h1>
<hr />
<div class="container">
  <p>
      Logged as <i><%= @user.name if @user %></i>
  </p>
  <%= render 'form' %>
</div>

<%= link_to 'Back', projects_path, class: "btn btn-default" %>
